# Deployment with Spring Boot Embedded Server
1. Create a jar file by using Maven Build --> clean install
2. A jar file will be created in targe folder.
3. In the jar file tomcat is already embedded because of spring framework starter dependency.
4. Run the jar file using <b>java -jar <<jarfilename>> </b>
5. You can browse the application on browser.
